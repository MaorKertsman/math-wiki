---
tags:
  - math/infi/claims
aliases:
  - הכללת קושי לסדר גבוה
  - generalized Cauchy MVT
---

# הכללת משפט קושי

## המשפט

יהיו $h,g:I\to\mathbb{R}$ רציפות ב-$I$ וגזירות $n+1$ פעמים ב-$I^o$. תהי $x_0\in I^o$ נקודה פנימית המקיימת:

1. $h^{(k)}(x_0)=0$ לכל $k=0,1,\ldots,n$.
2. $g^{(k)}(x_0)=0$ לכל $k=0,1,\ldots,n$.
3. $g^{(n+1)}(x)\neq 0$ לכל $x\in I^o\setminus\{x_0\}$.

אז לכל $x_1\in I$ קיימת $c$ ממש בין $x_0$ ל-$x_1$ כך ש:
$$
\frac{h(x_1)}{g(x_1)} = \frac{h^{(n+1)}(c)}{g^{(n+1)}(c)}
$$

## הוכחה

נגדיר $\varphi:I\to\mathbb{R}$ על ידי:
$$
\varphi(x) = h(x)\cdot g^{(n+1)}(x_0)\cdot\text{(קבוע)} - g(x)\cdot h^{(n+1)}(x_0)\cdot\text{(קבוע)}
$$

עדיף: נגדיר ישירות $\varphi(x) = h(x_1)g(x) - g(x_1)h(x)$.

**תכונות של $\varphi$:**
- $\varphi(x_0) = h(x_1)g(x_0) - g(x_1)h(x_0) = h(x_1)\cdot 0 - g(x_1)\cdot 0 = 0$.
- $\varphi(x_1) = h(x_1)g(x_1) - g(x_1)h(x_1) = 0$.
- $\varphi^{(k)}(x_0) = h(x_1)g^{(k)}(x_0) - g(x_1)h^{(k)}(x_0) = 0$ לכל $k=1,\ldots,n$.

ניישם את **משפט קושי המוכלל** (או רול $n+1$ פעמים):

הפונקציה $\varphi$ מתאפסת ב-$x_0$ (עם נגזרות מאפסות עד סדר $n$) וב-$x_1$. מרול המוכלל: קיים $c_1$ בין $x_0$ ל-$x_1$ עם $\varphi^{(1)}(c_1)=0$. אך $\varphi^{(1)}$ מתאפסת גם ב-$x_0$ (עם נגזרות עד סדר $n-1$). מרול שוב: קיים $c_2$ בין $x_0$ ל-$c_1$ עם $\varphi^{(2)}(c_2)=0$... נמשיך $n+1$ פעמים:

קיים $c=c_{n+1}$ ממש בין $x_0$ ל-$x_1$ עם $\varphi^{(n+1)}(c)=0$:
$$
0 = \varphi^{(n+1)}(c) = h(x_1)g^{(n+1)}(c) - g(x_1)h^{(n+1)}(c)
$$

כי $g^{(n+1)}(c)\neq 0$ (תנאי 3), נחלק:
$$
h(x_1) = g(x_1)\cdot\frac{h^{(n+1)}(c)}{g^{(n+1)}(c)}
$$

ונחלק ב-$g(x_1)$ (שגם אינו אפס — כי $g$ מתאפסת רק עד הנגזרת $n$ ב-$x_0$ אך לא בהכרח ב-$x_1$):
$$
\frac{h(x_1)}{g(x_1)} = \frac{h^{(n+1)}(c)}{g^{(n+1)}(c)}
$$
■

## שימוש

משפט זה הוא הבסיס להוכחת [[כלל לופיטל]] עבור גבולות מסוג $0/0$ כאשר הנגזרות הראשונות מתאפסות, ולהוכחה של [[שארית בצורת לגרנז']] מהצורה הכללית.

## ראה גם

- [[משפט קושי]]
- [[כלל לופיטל]]
- [[שארית בצורת לגרנז']]
